# 动态任务系统 - 集成指南

## 一、文件清单

以下是本次新增的全部文件及其用途：

| 文件名 | 用途 | 导出全局变量 |
|--------|------|-------------|
| `quest-engine.js` | 任务引擎核心，包含任务状态机、注册表管理、步骤推进、奖励发放等核心逻辑 | `window.QuestEngine` |
| `quest-app.js` | 手机端 UI 界面，提供任务列表、任务详情、操作交互等完整的 iOS 风格界面 | `window.QuestApp` |
| `quest-app.css` | UI 样式表，iOS 风格设计，靛蓝主色调，与 task-app.css 风格保持一致 | — |
| `quest-planner-bridge.js` | ENA Planner 桥接模块，连接小白X的 ENA Planner 与动态任务系统，负责 AI 任务生成、世界书同步 | `window.QuestPlannerBridge` |
| `动态任务系统技术方案.md` | 技术方案文档，包含完整的架构设计、变量协议、状态机定义等 | — |

---

## 二、集成步骤

### 步骤1：复制文件

将以下 4 个文件复制到 `mobile-plugin/mobile/` 目录下：

```
quest-engine.js
quest-app.js
quest-app.css
quest-planner-bridge.js
```

确保文件编码为 UTF-8（无 BOM）。

---

### 步骤2：修改 phone-loader.js

在 `phone-loader.js` 中进行两处修改：

#### 2.1 在 MODULES 数组中添加 3 个新模块

在 `independent-ai` 模块**之前**插入以下条目：

```javascript
{ name: 'quest-engine',         src: 'quest-engine.js',         globalVar: 'QuestEngine',         deps: ['bridge-api'] },
{ name: 'quest-planner-bridge', src: 'quest-planner-bridge.js', globalVar: 'QuestPlannerBridge', deps: ['quest-engine', 'xiaobaix-bridge', 'role-api'] },
{ name: 'quest-app',            src: 'quest-app.js',            globalVar: 'QuestApp',            deps: ['quest-engine', 'quest-planner-bridge'] },
```

**依赖关系说明：**

- `quest-engine` 依赖 `bridge-api`（用于变量读写）
- `quest-planner-bridge` 依赖 `quest-engine`（任务引擎）、`xiaobaix-bridge`（世界书 CRUD）、`role-api`（角色信息）
- `quest-app` 依赖 `quest-engine` 和 `quest-planner-bridge`（UI 展示与交互）

#### 2.2 在 CSS_FILES 数组中添加样式文件

```javascript
'quest-app.css'
```

---

### 步骤3：修改 mobile-phone.js

在手机桌面的应用图标区域，找到"任务"图标（原 TaskApp 的点击事件），将其点击处理函数修改为打开 `QuestApp`。

**修改前（示例）：**

```javascript
// 原来的任务图标点击
icon.addEventListener('click', function() {
    if (window.TaskApp) {
        window.TaskApp.open();
    }
});
```

**修改后：**

```javascript
// 新的动态任务系统
icon.addEventListener('click', function() {
    if (window.QuestApp) {
        window.QuestApp.open();
    } else if (window.TaskApp) {
        // 降级兼容：如果 QuestApp 未加载，回退到旧版
        window.TaskApp.open();
    }
});
```

> **兼容说明：** 保留 `TaskApp` 降级逻辑，确保在 `QuestApp` 加载失败时仍可使用旧版任务功能。

---

### 步骤4：修改 independent-ai.js

在 Orchestrator 初始化中，添加对 `xb.quest.pendingNotify` 变量的消费逻辑。当任务完成时，桥接模块会将通知写入该变量，Orchestrator 需要读取并处理。

**在 Orchestrator 的消息处理循环中添加：**

```javascript
// 检查任务完成通知
try {
    var pendingNotify = window.BridgeAPI.getVariable('xb.quest.pendingNotify');
    if (pendingNotify) {
        var notify = typeof pendingNotify === 'string' ? JSON.parse(pendingNotify) : pendingNotify;

        if (notify && notify.type === 'quest_completed') {
            // 将任务完成信息注入到 Orchestrator 上下文中
            // 让 AI 在回复中自然提及任务完成
            var questContext = '[系统通知: 玩家刚完成了任务「' + notify.questName + '」(' +
                (notify.questType || 'side') + ')]';

            // 根据你的 Orchestrator 实现，将 questContext 注入到合适的上下文位置
            // 例如追加到 system prompt 或作为特殊消息插入

            // 消费完毕后清除通知
            window.BridgeAPI.setVariable('xb.quest.pendingNotify', '');
        }
    }
} catch (e) {
    console.warn('[Orchestrator] 读取任务通知失败:', e);
}
```

**通知数据结构：**

```json
{
    "type": "quest_completed",
    "questId": "quest_1714000000000",
    "questName": "探索神秘森林",
    "questType": "side",
    "timestamp": 1714000000000
}
```

---

### 步骤5：配置小白X循环任务

通过小白X的循环任务功能，定期触发动态任务生成。以下是一个示例配置：

**循环任务名称：** `动态任务生成器`

**触发条件：** 每隔 5 分钟（或每次对话轮次）

**执行脚本：**

```javascript
// 检查 QuestPlannerBridge 是否可用
if (window.QuestPlannerBridge && window.QuestPlannerBridge.isReady()) {
    // 获取当前活跃任务数
    var activeQuests = window.QuestEngine.getActiveQuests();
    var maxActive = window.QuestPlannerBridge.config.maxActiveQuests || 10;

    // 如果活跃任务数未达上限，触发新任务生成
    if (activeQuests.length < maxActive) {
        window.QuestPlannerBridge.generateNewQuests()
            .then(function(newQuests) {
                if (newQuests && newQuests.length > 0) {
                    console.log('[循环任务] 生成了 ' + newQuests.length + ' 个新任务');
                }
            })
            .catch(function(err) {
                console.warn('[循环任务] 生成失败:', err);
            });
    } else {
        console.log('[循环任务] 活跃任务已达上限 (' + activeQuests.length + '/' + maxActive + ')');
    }
} else {
    console.log('[循环任务] QuestPlannerBridge 尚未就绪');
}
```

**替代方案 —— 基于对话轮次触发：**

如果使用对话轮次触发而非定时器，可以将上述脚本放入小白X的"每轮对话后执行"钩子中，并添加频率限制：

```javascript
// 频率限制：每 10 轮对话最多触发一次
var lastGenerateTurn = window.BridgeAPI.getVariable('xb.quest.lastGenerateTurn') || 0;
var currentTurn = window.BridgeAPI.getVariable('xb.game.turnCount') || 0;

if (currentTurn - lastGenerateTurn >= 10) {
    window.BridgeAPI.setVariable('xb.quest.lastGenerateTurn', currentTurn);
    // ... 执行生成逻辑（同上）
}
```

---

## 三、变量协议汇总

动态任务系统使用以下 `xb.quest.*` 命名空间的变量进行数据持久化和模块间通信：

| 变量路径 | 类型 | 读/写 | 说明 |
|----------|------|-------|------|
| `xb.quest.registry` | JSON 字符串 | R/W | 任务注册表，包含所有已注册任务的元信息（ID、名称、类型、状态等） |
| `xb.quest.active.{questId}` | JSON 字符串 | R/W | 指定任务的完整活跃状态（步骤进度、当前步骤、开始时间等） |
| `xb.quest.result.{questId}` | JSON 字符串 | R/W | 指定任务的结果记录（完成时间、获得奖励、评分等） |
| `xb.quest.pendingAction` | JSON 字符串 | R/W | 待处理的用户操作（UI 交互产生的操作请求，如选择分支、确认奖励等） |
| `xb.quest.pendingNotify` | JSON 字符串 | R/W | 待处理的系统通知（任务完成通知，供 independent-ai.js 的 Orchestrator 消费） |
| `xb.quest.lastUpdate` | 数字 | R/W | 最后更新时间戳（毫秒），用于缓存失效判断 |
| `xb.quest.lastCompletedResult` | 字符串 | R/W | 最近一次完成的任务结果摘要文本 |
| `xb.quest.activeSummary` | 字符串 | R/W | 当前所有活跃任务的摘要文本，供世界书和 AI 上下文使用 |
| `xb.quest.planner.config` | JSON 对象 | R/W | QuestPlannerBridge 的运行时配置（可覆盖默认值） |
| `xb.quest.templates` | JSON 数组 | R/W | 任务模板（备用存储，当世界书不可用时使用） |
| `xb.quest.lastGenerateTurn` | 数字 | R/W | 上次触发任务生成的对话轮次（用于频率限制） |

**变量大小控制建议：**

- `xb.quest.registry`：建议控制在 10KB 以内（约 50 个任务条目）
- `xb.quest.active.{questId}`：单个任务状态建议不超过 2KB
- `xb.quest.activeSummary`：摘要文本建议不超过 500 字符
- 定期调用 `QuestEngine.cleanupCompleted()` 清理已完成/已归档的任务，释放空间

---

## 四、世界书配置示例

### 4.1 任务状态同步条目（自动创建）

QuestPlannerBridge 会在首次运行时自动创建以下世界书条目，用于将当前任务状态注入 AI 上下文：

| 字段 | 值 |
|------|-----|
| **UID** | `quest_planner_active_status` |
| **Key** | `手机任务/当前任务/quest` |
| **Comment** | `ENA Planner 任务状态同步` |
| **Constant** | `true` |
| **Selective** | `false` |
| **Position** | `before_char`（角色定义之前） |
| **Insertion Order** | `100` |
| **Enabled** | `true` |
| **Exclude Recursion** | `true` |
| **Prevent Recursion** | `true` |

**自动生成的内容示例：**

```
{{char}}当前的手机任务状态：
【主线】探索神秘森林 - 进行中 (步骤 2/3: 与精灵对话)
【支线】收集草药 - 进行中 (步骤 1/2: 前往药草园)
【日常】每日锻炼 - 可接取

基于此在剧情中自然提及相关任务。
```

### 4.2 任务模板条目（手动配置）

可在世界书中手动添加任务模板条目，供 AI 生成任务时参考。条目 Key 以 `任务模板/` 或 `quest_template/` 开头即可被自动识别。

**示例条目 1：**

| 字段 | 值 |
|------|-----|
| **Key** | `任务模板/探索类` |
| **Comment** | `探索类任务模板` |
| **Content** | 见下方 |
| **Constant** | `false` |
| **Enabled** | `true` |

```json
{
  "templateId": "explore_basic",
  "namePattern": "探索{location}",
  "type": "side",
  "description": "前往{location}进行探索，发现隐藏的秘密。",
  "steps": [
    {
      "type": "travel",
      "description": "前往{location}",
      "interaction": "goto",
      "target": "{location}"
    },
    {
      "type": "investigate",
      "description": "在{location}中调查可疑之处",
      "interaction": "confirm"
    },
    {
      "type": "select",
      "description": "选择如何处理发现的线索",
      "interaction": "select",
      "options": ["带走线索", "通知{charName}", "继续深入调查"]
    }
  ],
  "rewards": [
    { "type": "variable", "path": "xb.player.exp", "op": "+", "value": 50 }
  ],
  "conditions": {
    "requiredVariables": []
  }
}
```

**示例条目 2：**

| 字段 | 值 |
|------|-----|
| **Key** | `任务模板/对话类` |
| **Comment** | `对话类任务模板` |
| **Content** | 见下方 |
| **Constant** | `false` |
| **Enabled** | `true` |

```json
{
  "templateId": "dialogue_basic",
  "namePattern": "与{npcName}交谈",
  "type": "side",
  "description": "找到{npcName}并与其进行一次深入的对话。",
  "steps": [
    {
      "type": "travel",
      "description": "前往{npcName}所在地点",
      "interaction": "goto",
      "target": "{npcLocation}"
    },
    {
      "type": "dialogue",
      "description": "与{npcName}交谈",
      "interaction": "confirm"
    }
  ],
  "rewards": [
    { "type": "relationship", "target": "{npcName}", "op": "+", "value": 10 }
  ]
}
```

### 4.3 触发关键词条目（可选）

如果希望 AI 在特定剧情关键词出现时触发任务生成，可添加以下条目：

| 字段 | 值 |
|------|-----|
| **Key** | `任务触发/新任务` |
| **Secondary Keys** | `新任务,接受任务,发布任务,悬赏` |
| **Comment** | `检测任务相关关键词` |
| **Content** | 见下方 |
| **Constant** | `false` |
| **Selective** | `true` |
| **Enabled** | `true` |

```
[检测到任务相关对话，建议检查是否需要生成新的动态任务]
{{char}}正在与玩家讨论任务相关的话题。
```

---

## 五、测试方法

### 5.1 手动测试步骤

#### 基础功能测试

1. **模块加载验证**
   - 打开手机界面，确认控制台无报错
   - 在控制台输入 `window.QuestEngine`、`window.QuestApp`、`window.QuestPlannerBridge`，确认三个对象均已定义

2. **UI 打开测试**
   - 点击手机桌面上的"任务"图标
   - 确认 QuestApp 界面正常显示（任务列表、筛选标签、底部导航）

3. **手动注册任务**
   - 在控制台执行：
     ```javascript
     QuestEngine.registerQuest({
         name: '测试任务',
         description: '这是一个测试任务',
         type: 'side',
         priority: 'medium',
         steps: [
             { type: 'travel', description: '前往测试地点', interaction: 'goto', target: '测试地点' },
             { type: 'confirm', description: '确认完成', interaction: 'confirm' }
         ],
         rewards: [
             { type: 'variable', path: 'xb.player.money', op: '+', value: 100 }
         ]
     });
     ```
   - 确认任务出现在 UI 列表中，状态为"可接取"

4. **任务接取与推进**
   - 在 UI 中点击"接取"按钮
   - 确认任务状态变为"进行中"
   - 逐步完成每个步骤，确认步骤进度正确更新

5. **任务完成与奖励**
   - 完成所有步骤后，确认进入"待领奖"状态
   - 点击"领取奖励"按钮
   - 确认奖励已发放（检查对应变量值变化）

#### AI 生成测试

6. **自动生成触发**
   - 确保循环任务已配置（见步骤5）
   - 等待触发条件满足，或手动执行生成脚本
   - 确认 AI 生成的任务被正确注册到引擎中

7. **世界书同步验证**
   - 完成一次任务生成或状态变更后
   - 打开世界书编辑器，检查 `手机任务/当前任务/quest` 条目内容是否已更新

### 5.2 控制台调试命令

```javascript
// ===== 基础检查 =====

// 检查所有模块是否加载
console.log('QuestEngine:', !!window.QuestEngine);
console.log('QuestApp:', !!window.QuestApp);
console.log('QuestPlannerBridge:', !!window.QuestPlannerBridge);

// ===== 任务引擎调试 =====

// 获取所有已注册任务
QuestEngine.getAllQuests();

// 获取活跃任务列表
QuestEngine.getActiveQuests();

// 获取指定任务状态
QuestEngine.getQuestState('quest_1714000000000');

// 获取任务注册表原始数据
BridgeAPI.getVariable('xb.quest.registry');

// 获取最后更新时间
BridgeAPI.getVariable('xb.quest.lastUpdate');

// 手动清理已完成/已归档任务
QuestEngine.cleanupCompleted();

// ===== 桥接模块调试 =====

// 检查桥接模块是否就绪
QuestPlannerBridge.isReady();

// 查看当前配置
QuestPlannerBridge.config;

// 手动触发任务生成
QuestPlannerBridge.generateNewQuests()
    .then(function(quests) { console.log('生成结果:', quests); })
    .catch(function(err) { console.error('生成失败:', err); });

// 查看活跃任务摘要
BridgeAPI.getVariable('xb.quest.activeSummary');

// 查看上次完成结果
BridgeAPI.getVariable('xb.quest.lastCompletedResult');

// 查看待处理通知
BridgeAPI.getVariable('xb.quest.pendingNotify');

// 手动触发世界书同步
QuestPlannerBridge.updateWorldbookWithQuestInfo()
    .then(function() { console.log('世界书同步完成'); })
    .catch(function(err) { console.error('同步失败:', err); });

// ===== UI 调试 =====

// 打开任务界面
QuestApp.open();

// 关闭任务界面
QuestApp.close();

// 强制刷新任务列表
QuestApp.refresh();

// ===== 变量协议调试 =====

// 查看所有 quest 相关变量
Object.keys(window.BridgeAPI.getAllVariables())
    .filter(function(k) { return k.indexOf('xb.quest.') === 0; })
    .forEach(function(k) {
        console.log(k + ':', BridgeAPI.getVariable(k));
    });
```

---

## 六、注意事项

### 6.1 与旧版 task-app 的兼容

- **共存策略**：新版动态任务系统（QuestApp）与旧版 TaskApp 可以共存。`quest-engine.js` 使用独立的 `xb.quest.*` 变量命名空间，不会与旧版 `xb.task.*` 变量冲突。
- **图标切换**：在 `mobile-phone.js` 中修改"任务"图标点击事件时，建议保留 `TaskApp` 的降级逻辑（见步骤3），确保向后兼容。
- **渐进迁移**：可以先让两个系统并行运行，确认新版稳定后再完全替换旧版。移除旧版时需同步清理 `xb.task.*` 相关变量和旧版世界书条目。

### 6.2 变量大小控制

- **注册表膨胀**：`xb.quest.registry` 会随着任务增多而增大。建议将 `maxRegistrySize` 保持在 50 以内，并定期调用 `QuestEngine.cleanupCompleted()` 清理已完成/已归档的任务。
- **活跃任务数量**：通过 `QuestPlannerBridge.config.maxActiveQuests`（默认 10）控制同时进行的任务数量上限。
- **摘要长度**：`xb.quest.activeSummary` 会被注入到世界书中供 AI 消费，过长的摘要会占用大量 token。建议活跃任务不超过 5 个，每个任务的摘要描述控制在 50 字以内。
- **变量清理**：已完成的任务结果（`xb.quest.result.{questId}`）在确认不需要后应手动清理，避免变量空间浪费。

### 6.3 API 调用频率

- **变量读写**：任务状态变更时会触发多次 `BridgeAPI.setVariable` 调用。在快速连续操作（如批量推进步骤）时，注意避免过于频繁的写入。引擎内部已实现 5 秒缓存 TTL（`cacheTTL: 5000`）来减少重复读取。
- **AI 生成频率**：自动生成间隔默认为 5 分钟（`generateInterval: 300000`），最小间隔为 30 秒（`MIN_AUTO_INTERVAL`）。不建议将间隔设置低于 60 秒，以避免 API 调用过于频繁。
- **世界书更新**：每次任务状态变更都会触发世界书同步。如果任务操作非常频繁，世界书更新可能会成为性能瓶颈。可通过将 `QuestPlannerBridge.config.enabled` 设为 `false` 临时禁用桥接模块。
- **循环任务触发**：如果使用小白X循环任务触发生成，建议设置合理的频率限制（如每 10 轮对话一次），避免在快速对话场景中过度生成任务。

### 6.4 其他注意事项

- **WebView 兼容性**：所有代码均使用 ES5 语法（IIFE + var/function），不依赖 ES Module、optional chaining、nullish coalescing 等新特性，确保在 Android WebView 中正常运行。
- **错误处理**：各模块内部已实现 try-catch 保护，单个模块异常不会导致整个手机系统崩溃。异常信息会通过 `console.warn` 输出，可在控制台中查看。
- **数据持久化**：所有任务数据通过 `BridgeAPI` 持久化到 SillyTavern 变量系统中。清除浏览器缓存不会丢失任务数据，但重置角色卡或清除全局变量会导致数据丢失。
- **时区与时间**：任务超时检查使用 `Date.now()` 毫秒时间戳，不受时区影响。游戏内时间通过 `xb.game.time` 变量获取，需确保该变量被正确维护。
